import React from 'react';
import { Button, Typography } from '@mui/material';

const HelpRequest = () => (
  <div>
    <Typography variant="h6">담당 정류장 안내원과 전화 연결 중입니다. 잠시만 기다려주세요.</Typography>
    <Button variant="contained" color="secondary">통화 연결</Button>
  </div>
);

export default HelpRequest;
